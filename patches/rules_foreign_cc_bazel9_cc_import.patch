diff --git a/toolchains/built_toolchains.bzl b/toolchains/built_toolchains.bzl
--- a/toolchains/built_toolchains.bzl
+++ b/toolchains/built_toolchains.bzl
@@ -49,6 +49,8 @@ def built_toolchains(cmake_version, make_version, ninja_version, meson_version,
 
     if register_built_pkgconfig_toolchain:
         _pkgconfig_toolchain(pkgconfig_version, register_toolchains)
+    else:
+        _pkgconfig_toolchain(pkgconfig_version, False)
 
 def _cmake_toolchain(version, register_toolchains):
     if register_toolchains:
@@ -269,3 +271,4 @@ def _pkgconfig_toolchain(version, register_toolchains):
         name = "glib_dev",
         build_file_content = '''
+load("@rules_cc//cc:defs.bzl", "cc_import")
 cc_import(
@@ -287,3 +290,4 @@ def _pkgconfig_toolchain(version, register_toolchains):
         name = "glib_src",
         build_file_content = '''
+load("@rules_cc//cc:defs.bzl", "cc_import")
 cc_import(
@@ -326,3 +330,4 @@ def _pkgconfig_toolchain(version, register_toolchains):
         name = "gettext_runtime",
         build_file_content = '''
+load("@rules_cc//cc:defs.bzl", "cc_import")
 cc_import(
diff --git a/foreign_cc/extensions.bzl b/foreign_cc/extensions.bzl
--- a/foreign_cc/extensions.bzl
+++ b/foreign_cc/extensions.bzl
@@ -21,7 +21,7 @@ def _init(module_ctx):
         register_built_tools = True,
         register_default_tools = False,
         register_preinstalled_tools = False,
-        register_built_pkgconfig_toolchain = True,
+        register_built_pkgconfig_toolchain = False,
         # These should be registered via bzlmod entries instead
         register_repos = False,
     )
diff --git a/foreign_cc/built_tools/pkgconfig_build.bzl b/foreign_cc/built_tools/pkgconfig_build.bzl
--- a/foreign_cc/built_tools/pkgconfig_build.bzl
+++ b/foreign_cc/built_tools/pkgconfig_build.bzl
@@ -161,11 +161,11 @@ def pkgconfig_tool(name, srcs, **kwargs):
         lib_source = srcs,
         args = [
             "-f Makefile.vc",
             "CFG=release",
-            "GLIB_PREFIX=\"$$EXT_BUILD_ROOT/external/glib_dev\"",
+            "GLIB_PREFIX=\"$$EXT_BUILD_ROOT/external/rules_foreign_cc++tools+glib_dev\"",
         ],
         out_binaries = ["pkg-config.exe"],
-        env = {"INCLUDE": "$$EXT_BUILD_ROOT/external/glib_src"},
+        env = {"INCLUDE": "$$EXT_BUILD_ROOT/external/rules_foreign_cc++tools+glib_src"},
         out_static_libs = [],
         out_shared_libs = [],
         deps = [
