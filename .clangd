CompileFlags:
  Add:
    - "-std=c++20"
    - "-Wall"
    - "-I."
  # Bazel manages include paths; suppress missing-header false positives
  # until compile_commands.json is generated with:
  #   bazel run @hedron_compile_commands//:refresh_all
